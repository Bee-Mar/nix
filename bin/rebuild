#!/usr/bin/env bash

system=$(cat /sys/class/dmi/id/sys_vendor | awk '{print tolower($0)}' | tr -d '[:punct:]' | awk '{print $1}')
sudo nixos-rebuild switch -I nixos-config=$HOME/.config/nix/configuration.nix --flake .#${system} "$@"
